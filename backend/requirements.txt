# RoomTint Backend Requirements
# All packages install from PyPI once, then work offline.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# SAM (local model file — no internet after install)
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git

# Mask2Former (local cache after download_models.py)
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
accelerate>=0.30.0

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Database
psycopg2-binary>=2.9.9

# Utilities
python-dotenv>=1.0.0
anyio>=4.0.0
